<h1>Listing comments</h1>
<br>

<br>
<!--display comments in new way??? -->
<h2>Updates:</h2>
  <br>
    <table class = "table table-striped">
    <% @comments.each do |comment| %>
    <tr>
      <td>Post: <strong><%= comment.title %></strong>
      <br>
      <%= comment.content %>
      <br>
      <%= link_to 'Show', comment %> <%= link_to 'Edit', edit_comment_path(comment) %> <%= link_to 'Destroy', comment, confirm: 'Are you sure?', method: :delete %>
      </td>
    </tr>
  <% end %>
</table>

<br>

<%= link_to 'New Comment', new_comment_path %>
<br>
<p>
  This is a link back to the home page <%= link_to "Home", static_pages_home_path %>
</p>